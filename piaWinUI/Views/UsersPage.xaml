<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="piaWinUI.Views.UsersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:piaWinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:models="using:piaWinUI.Models"
    mc:Ignorable="d"
    xmlns:converters="using:piaWinUI.Converters"
    Background="#0F172A">

    <Page.Resources>
        <converters:PasswordMaskConverter x:Key="PasswordMaskConverter"/>
        <converters:AdminDisplayConverter x:Key="AdminDisplayConverter"/>
    </Page.Resources>


    <Grid>

        <controls:DataGrid
        ItemsSource="{x:Bind Users}"
        AutoGenerateColumns="False"
        IsReadOnly="True"
        Margin="10"
        CanUserResizeColumns="False"
        CanUserReorderColumns="False">

            <controls:DataGrid.Columns>

                <controls:DataGridTextColumn
                Header="Usuario"
                Binding="{Binding Username}" />

                <controls:DataGridTextColumn
                Header="Contraseña"
                Binding="{Binding Password, Converter={StaticResource PasswordMaskConverter}}" />

                <controls:DataGridTextColumn
                    Header="Rol"
                    Binding="{Binding IsAdmin, Converter={StaticResource AdminDisplayConverter}}" />

                <controls:DataGridTemplateColumn Header="Acción">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="8" Margin="5">

                                <Button
                                Content="Editar"
                                Click="EditUser_Click"/>

                                <Button Content="Eliminar"
                                Click="DeleteUser_Click"
                                IsEnabled="{Binding CanDeleteUsers}"/>

                            </StackPanel>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <StackPanel Orientation="Horizontal"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="10">
           
            <Button Content="Nuevo usuario"
                Click="AddUser_Click"
                    Margin="0 0 10 10"/>

            <Button Content="Recargar" Click="Reload_Click"
                    Margin="0 0 0 10"/>
            
        </StackPanel>
    </Grid>
    
</Page>
